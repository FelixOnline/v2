[
  {{ range $i, $e := where .Site.Pages "Type" "articles" }}
    {{ if $i }}, {{ end }}
    {
      "title": {{ jsonify (trim .Title "\n") }},
      "subtitle": {{ jsonify (trim .Params.subtitle "\n") }},
      "link": "{{ .Permalink }}",
      "image": "{{ .Params.image }}"
    }
  {{ end }}
]
